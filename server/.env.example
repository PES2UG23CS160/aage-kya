# ─── Server (Express) environment variables ──────────────────────────────────
# Copy this file to server/.env and fill in real values.
# NEVER commit server/.env — it is in .gitignore.

# Render/production must set this explicitly so fail-closed validation and
# strict CORS are enabled. Keep development for a local copy.
NODE_ENV=development

# Port for the Express server (default: 5000)
PORT=5000

# Allowed frontend origins (comma-separated) for CORS.
# Set this to your deployed Vercel URL in production.
# Leave blank/unset only in local development. Production startup rejects an
# empty allowlist.
# Example: ALLOWED_ORIGINS=https://aage-kya.vercel.app,https://www.aagekya.com
# ALLOWED_ORIGINS=

# Public web origin used in transactional links. Defaults to the first allowed
# origin when omitted.
# PUBLIC_APP_URL=https://aage-kya.vercel.app

# Groq API key (server-side ONLY — never use VITE_ prefix for this)
# Get your FREE key from: https://console.groq.com → API Keys
# Model used: llama-3.3-70b-versatile (default) — override with GROQ_MODEL env var
GROQ_API_KEY=your_groq_api_key_here

# Supabase project credentials (same project as frontend)
# Get from: Supabase dashboard → Project Settings → API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (REQUIRED in production)
# Used for server-managed guidance/audit writes, mentor applications, analytics,
# and the optional development-only seed script.
# CAUTION: This key bypasses Row Level Security. Keep it server-side only.
# Get from: Supabase dashboard → Project Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ─── Phase 3: RAG Data Setup ──────────────────────────────────────────────────
# After filling in the keys above and running supabase_schema.sql:
#   cd server && node seed.js
# WARNING: the current seed file contains prototype data that has not completed
# the provenance review described in docs/DATA_AND_AI_RESEARCH.md. Do not load
# it into production or describe it as verified.
# To seed a disposable development database only, explicitly set:
# ENABLE_PROTOTYPE_DATA=true

# ─── Optional: Override AI model ─────────────────────────────────────────────
# Default model: llama-3.3-70b-versatile
# Other fast Groq options: llama-3.1-8b-instant, mixtral-8x7b-32768
# GROQ_MODEL=llama-3.3-70b-versatile

# Provider gateway controls. Structured guidance calls time out and retry only
# transient failures before a configured fallback provider is attempted.
AI_TIMEOUT_MS=30000
AI_MAX_RETRIES=1

# Required in production. The sender must belong to a domain verified in Resend.
# RESEND_API_KEY=your_resend_api_key_here
# RESEND_FROM_EMAIL=Aage Kya <noreply@example.org>
